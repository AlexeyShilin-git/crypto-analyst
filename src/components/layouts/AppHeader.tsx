import { Layout } from "antd";

const { Header } = Layout;

export default function AppHeader() {
  return (
    <Header className="text-center bg-cyan-950 text-amber-50 leading-[4rem]">
      Header
    </Header>
  );
}
